<% @creatures.each do |creature| %>

    <div>
        <div>
          <%= link_to creature.name, creature_path(creature.id) %>
        </div>
        Name: <%= creature.name %> <br>
        Description: <%=  creature.description %>
        <%= button_to "Delete", creature, method: :delete %>
    </div>

<% end %>